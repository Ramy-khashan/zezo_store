import 'package:flutter/material.dart';

class AppColors {
  static const Color blackColor = Color(0xff09080d);
  static const Color whiteColor = Colors.white;
  static const Color primaryColor = Color.fromARGB(255, 12, 16, 55);
}
